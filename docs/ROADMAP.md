# Roadmap (POC)

## v0.1 — Scaffold
- Repo structure + docs
- SQL scripts placeholders
- Python service skeletons
- React dashboard skeleton

## v0.2 — End-to-end POC
- Schema + seed data
- Read-heavy + write-heavy workload scripts
- Collector pulls Query Store + key DMVs into feature tables
- Recommender outputs top 1–3 CREATE INDEX suggestions
- Before/after report

## v0.3 — ML extension (optional)
- Hot query/table forecasting
- Proactive recommendation simulation (no auto-apply in prod)
